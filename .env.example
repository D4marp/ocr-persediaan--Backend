APP_PORT=8080

# Database
DB_HOST=mysql
DB_PORT=3306
DB_USER=user
DB_PASSWORD=password
DB_NAME=persediaan_db

PROCESSING_SERVICE_URL=http://127.0.0.1:8090
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10

# docker-compose.yml host port overrides (optional - defaults shown below
# match the production VPS deployment; override if these are ever taken too).
BACKEND_PORT=8082
FRONTEND_PORT=3002
MYSQL_HOST_PORT=3309
PUBLIC_API_URL=http://103.27.207.58:8082/api/v1

# DB_PASSWORD above is a placeholder. Set a real password in your own
# untracked .env before deploying — never commit a real credential here.
